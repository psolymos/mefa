- list of matrices as mefa segments -> mefa object

methods:

matrix, data.frame, list, array, mefa, stcs

as.mefa.list
as.mefa.array
as.mefa.matrix
as.mefa.data.frame

as.stcs.list
as.stcs.array
as.stcs.matrix
as.stcs.data.frame

as.matrix.mefa et al ???

Related
done - JSS paper revison
dole - vignette and demo revision based on JSS paper

Code
done - add labels for image method
done - hide default plot arguments by using missing() inside
* - use %*% in aggregate and check speed increase
    * character/object separation and model.matrix further complicates this
* - somehow fix inside/outside recognition in aggregate
    *: only possible with formula, and in that way, these must be factors to be able to do matrix multiplication

Not mefa
done - remove separateint from abmi
done - update permatsar in sedar/mefaspat
